query1: CREATE TABLE IF NOT EXISTS rooms (id INT PRIMARY KEY, name VARCHAR(255))

query2: CREATE TABLE IF NOT EXISTS students (birthday VARCHAR(255), id INT PRIMARY KEY,name VARCHAR(255), room INT, sex VARCHAR(1))

